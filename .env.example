# Application
NODE_ENV=development
APP_PORT=3080

# Database - Configuración optimizada para evitar errores de conexión
# Parámetros importantes:
# - connection_limit: Número máximo de conexiones en el pool (default: 10)
# - pool_timeout: Tiempo máximo de espera para obtener una conexión del pool en segundos (default: 10)
# - connect_timeout: Tiempo máximo de espera para establecer conexión en segundos (default: 5)
# - statement_timeout: Tiempo máximo de ejecución de una query en milisegundos (default: 0 = sin límite)
# - idle_in_transaction_session_timeout: Tiempo máximo que una transacción puede estar idle en milisegundos
DATABASE_URL=postgresql://user:password@localhost:5432/database_name?connection_limit=20&pool_timeout=30&connect_timeout=10&statement_timeout=60000&idle_in_transaction_session_timeout=60000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d

# Default Role
DEFAULT_ROLE_NAME=USER

# CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:3080

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret